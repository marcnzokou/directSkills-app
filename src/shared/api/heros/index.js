/**
 * API METHOD 
 */
import { hashToString } from '../../';

const config = CONFIG;
const hash = hashToString();

/**
 * GET ALL API
 */
const $getAll = () => {
    
    return h;
}

const heros = {
    $getAll
}
export default heros;